window {
    size(168,20)

    panel('','vertical') {
        bgthickness(2)
        textfield('name') {
            channel('name')
            desired(160,16)
            tooltips('Optional name of this', 'redstone channel')
        }
    }
}
